.swiper__wrapper
  // Swiper
  // Slider main container
  .swiper-container
    // Additional required wrapper
    .swiper-wrapper
      // Slides
      .swiper-slide
        img(src=require('@images/png/main.png')  alt="main")
      .swiper-slide 
        img(src=require('@images/png/main.png')  alt="main")
      .swiper-slide 
        img(src=require('@images/png/main.png')  alt="main")
    // If we need pagination
    .swiper-pagination
